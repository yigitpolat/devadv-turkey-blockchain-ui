applications:
    - name: devadv-turkey-blockchain-ui
      path: ./build
      buildpack: https://github.com/cloudfoundry/staticfile-buildpack.git
      random-route: true
      memory: 256M
      env:
        FORCE_HTTPS: true
